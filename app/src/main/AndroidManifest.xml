<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sadassignment5">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Sad Assignment 5"
        android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar"> <!-- Use MaterialComponents theme -->

        <activity
            android:name=".MainActivity"
            android:label="Sad Assignment 5"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar"
            android:exported="true"> <!-- Explicitly specify exported attribute -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
